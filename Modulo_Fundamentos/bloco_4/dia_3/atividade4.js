let size = 5;
let space = '';
let symbol = '*';

let meio = (size + 1) / 2;
let cesq = meio
let cdir = meio

for (let index = 0; index < meio; index +=1){
  for(let tindex = 0; tindex < size; tindex += 1){
    if(tindex<)
  }
}